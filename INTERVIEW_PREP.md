# 面试问答整理

这份文档整理的是当前项目里最容易被追问的几类问题。回答时建议先讲目标，再讲机制，再讲指标和风险。

## 12. Plan-Execute-Judge-Reflect + Memory

### 角色分工

- `Planner`
  - 输入：用户问题、schema、历史上下文、Memory 检索结果、业务约束
  - 输出：结构化 plan，不是长篇自然语言
- `Executer`
  - 输入：当前 step 目标、最近一轮 action/observation、schema 片段、Memory 命中结果、约束
  - 输出：实际 action、SQL、工具调用结果、观测信息
- `Judge`
  - 输入：当前输出、执行结果、plan、gold 或规则约束
  - 输出：一致性判断、是否继续、是否偏题、是否可执行
- `Reflect`
  - 输入：成功/失败轨迹、judge 结果、关键错误原因
  - 输出：写回 memory 的结构化经验摘要

### Planner 的 plan 长什么样

建议答成“结构化 JSON”而不是散文：

```json
{
  "task_id": "sql_67",
  "goal": "统计指定时间区间内指定道具的产出次数",
  "steps": [
    {
      "step_id": 1,
      "action": "probe",
      "target": "确认表和字段口径",
      "tool": "sql",
      "expected_signal": "igoodsid, addorreduce, tdbank_imp_date"
    },
    {
      "step_id": 2,
      "action": "execute",
      "target": "按道具id和时间范围统计产出次数",
      "tool": "sql",
      "stop_condition": "结果可直接回答问题"
    }
  ],
  "risk_flags": ["date_format_mismatch", "wrong_id_system"],
  "success_criteria": ["SQL可执行", "结果与gold一致"]
}
```

### Executer 每轮能看到哪些上下文

- 当前 step 的目标
- 最近一轮 action 和 observation
- 当前 schema 片段
- Memory 检索到的相似案例
- 必要的业务和安全约束

不建议把全量历史都塞进去，容易污染上下文。

### Judge 判断一致性时用哪些信号

- 结构信号：是否遵守协议，是否有 `<reasoning>/<sql>/<solution>`
- 执行信号：SQL 是否可执行、是否报错、是否空结果
- 结果信号：执行结果是否和 gold 一致
- 轨迹信号：当前 action 是否符合 plan，是否偏离目标

### Reflect 写回 memory 的是什么

写回的不是原始日志，而是“可复用经验”。推荐结构化 JSON：

```json
{
  "task_type": "text2sql",
  "pattern": "time_range + item_id_count",
  "failure_reason": "date_format_mismatch",
  "successful_action": "use tdbank_imp_date with YYYYMMDDHH range",
  "successful_sql_template": "SELECT igoodsid, COUNT(*) ...",
  "tags": ["probe", "date", "goods"]
}
```

Memory 里一般存的是：

- 模式摘要
- 失败原因
- 成功动作模板
- 检索标签

而不是完整对话。

## 12.1 数字怎么解释

### “结果准确率”怎么算

这里建议明确说成 `result_match_rate`：

- 先执行预测 SQL
- 对结果做归一化
- 再和 gold result 比较

比较时要考虑：

- 排序
- 去重
- 字段别名
- 空值处理

所以它不是字符串 exact match，而是“执行结果等价”。

### “相对官方 baseline 提升约 30%”是绝对还是相对

建议明确区分：

- 绝对提升：`0.20 -> 0.26`，提升 `6` 个百分点
- 相对提升：`(0.26 - 0.20) / 0.20 = 30%`

面试时优先报绝对值，再补相对值。

### 有没有做 ablation

更稳的表述是：

- 做过不同版本的对照，包括普通 final-SQL SFT、Agent 单轮 SFT、single-step GRPO、online multi-turn GRPO
- 从趋势上看，提升主要来自 agent 化监督和在线 RL，而不是单纯 prompt 变长
- 如果要强调“证明”，最好说“趋势支持”，不要说“严格证明”

## 13. 为什么做 RL，而不只堆 SFT

### 为什么值得做 RL

- SFT 学的是模仿分布
- RL 直接优化业务目标，比如执行正确率、结果匹配率、协议稳定性
- Text2SQL 里很多 SQL 都可能语义等价，但 SFT 不容易学到“哪种策略更稳”

### GRPO 的 reward 怎么设计

当前思路可以概括成四层：

- 格式奖励：有没有按协议输出
- 执行奖励：SQL 能不能跑通
- 结果奖励：执行结果和 gold 是否一致
- 轨迹奖励：是否更快收敛到正确答案，是否有效 probe

### 如果模型学会“投机”怎么办

- 加 KL 约束，防止策略偏离 SFT 太远
- 不要只奖励“能执行”，还要奖励“结果正确”
- 提高最终结果奖励，降低 trivial SQL 的奖励
- 对空查询、无信息查询、明显投机的输出做惩罚
- 用隐藏测试集和离线执行评测检查泛化
- 把失败案例回流到后续 SFT / reward 设计

一句话总结：

> reward 不能只奖励“能跑”，必须奖励“跑对”。

## 14. 司机助手 Agent 的评测框架

### 能力维度

- 意图理解
- 业务策略
- 工具调用
- 解释能力
- 风险控制
- 时效性

### 数据集构建

- 历史真实司机咨询
- 客服工单和运营会话
- 业务专家标注答案
- 高频场景
- 极端和对抗样本

### 自动评测和人工评测分工

- 自动评测看：
  - 任务成功率
  - 工具调用正确率
  - 策略命中率
  - 响应延迟
  - 合规命中率
- 人工评测看：
  - 策略是否合理
  - 解释是否清楚
  - 语气是否自然
  - 是否有业务常识漏洞

### 安全合规怎么测

- 违规引导测试
- 隐私和位置敏感场景
- 是否越权访问数据
- 是否输出误导性收益建议
- 是否建议不合规操作

### 上线后的回流闭环

- 收集线上对话、用户反馈、人工复核
- 自动聚类失败案例
- 分成理解错、工具错、策略错、表达错、合规错
- 回流到 SFT、reward、规则、prompt 四个层面
- 做灰度和 A/B 测试

## 15. 司机助手 Agent 的处理链路

以“我现在在机场排队，怎么提高今天流水？”为例：

### 推荐链路

1. 先做意图识别，判断这是策略建议类问题
2. 再补齐上下文，比如当前位置、排队状态、车型、在线时长、活动信息
3. 调系统接口获取实时数据，比如热区、排队、补贴、收益分布
4. 由规则引擎或策略引擎生成候选动作
5. 由 LLM 负责把结构化建议解释成人话

### 哪些交给 LLM

- 意图理解
- 补问缺失信息
- 候选方案解释
- 结构化建议转自然语言

### 哪些必须规则化或接业务接口

- 实时机场排队数据
- 收益计算
- 合规限制
- 路线、距离、时效、热度等数值逻辑

### 一句面试回答

> 我会把这类问题拆成三层：LLM 负责理解和表达，系统接口负责查实时事实，规则引擎负责把业务策略收敛成可执行动作。这样既能个性化，也能避免胡说。

## 你可以怎么收尾

- 数字一定讲口径
- 没做严格 ablation 就不要说“证明了”
- RL 的重点是“对齐业务目标”和“防止投机”
- 业务 Agent 的重点是“LLM 负责理解和表达，核心决策必须由系统兜底”
