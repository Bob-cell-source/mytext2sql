# 远端服务器访问数据库

这份文档说明如何让**远端训练/评测服务器**访问你**本地电脑上运行的 StarRocks 数据库**，从而完成：

- 远端模型生成 SQL
- 远端脚本执行 SQL
- 返回执行结果并做校验

推荐方式是：

- 本地启动 StarRocks
- 使用 `ssh -R` 建立**反向隧道**
- 让远端服务器通过本地回环地址访问映射端口

这种方式比直接把数据库端口暴露到公网更安全，也更适合你当前的开发测试场景。

## 1. 目标拓扑

目标是让远端服务器上的：

- `127.0.0.1:19030`

实际转发到你本地电脑上的：

- `127.0.0.1:9030`

也就是：

```text
远端服务器 127.0.0.1:19030
        ↓ SSH 反向隧道
本地电脑   127.0.0.1:9030
        ↓
StarRocks FE SQL 端口
```

## 2. 本地先启动 StarRocks

如果你已经有容器 `quickstart`：

```bash
docker start quickstart
docker ps
```

检查数据库是否真的可用：

```bash
docker exec -it quickstart mysql -P 9030 -h 127.0.0.1 -u root -e "SHOW DATABASES;"
docker exec -it quickstart mysql -P 9030 -h 127.0.0.1 -u root -e "USE TGAC; SHOW TABLES;"
```

如果这两条命令都能正常执行，说明你本地的 StarRocks 已经可用。

## 3. 从本地建立 SSH 反向隧道

在**本地电脑**上执行：

```bash
ssh -N -R 19030:127.0.0.1:9030 your_user@your_remote_server
```

解释：

- `-N`
  不执行远端命令，只建立隧道

- `-R 19030:127.0.0.1:9030`
  把远端服务器的 `127.0.0.1:19030`
  转发到本地电脑的 `127.0.0.1:9030`

执行后，这个 SSH 会话要一直保持在线。

## 4. 推荐用 tmux / screen 保持隧道

如果你担心隧道掉线，建议在本地开一个 `tmux` 会话再执行：

```bash
tmux new -s sr_tunnel
ssh -N -R 19030:127.0.0.1:9030 your_user@your_remote_server
```

分离会话：

```bash
Ctrl+b 然后按 d
```

恢复会话：

```bash
tmux attach -t sr_tunnel
```

## 5. 如果还需要映射 FE HTTP 端口

有时你还想在远端访问本地 StarRocks 的 FE HTTP 页面，可以一起映射：

```bash
ssh -N \
  -R 19030:127.0.0.1:9030 \
  -R 18030:127.0.0.1:8030 \
  your_user@your_remote_server
```

这样远端服务器上的：

- `127.0.0.1:19030` 对应本地 `9030`
- `127.0.0.1:18030` 对应本地 `8030`

## 6. 在远端服务器测试隧道是否打通

登录远端服务器后执行：

```bash
mysql -h 127.0.0.1 -P 19030 -u root -e "SHOW DATABASES;"
mysql -h 127.0.0.1 -P 19030 -u root -e "USE TGAC; SHOW TABLES;"
```

如果你远端没装 `mysql` 命令，也可以直接用项目脚本测试。

只要能看到：

- `TGAC`
- 对应业务表

说明隧道已经打通。

## 7. 在远端服务器设置项目环境变量

远端服务器上执行：

```bash
export DB_HOST="127.0.0.1"
export DB_PORT="19030"
export DB_USER="root"
export DB_PASSWORD=""
export DB_NAME="TGAC"
```

这里最关键的是：

- `DB_HOST=127.0.0.1`
- `DB_PORT=19030`

因为远端访问的并不是它自己的数据库，而是通过 SSH 隧道映射到你本地的数据库。

## 8. 远端执行 SQL 评测

在远端服务器上，你可以直接运行项目里的执行评测脚本：

```bash
python3 scripts/evaluate_sql_execution_from_report.py \
  --report-path output/eval_reports/vllm_action_eval.json \
  --output-path output/eval_reports/vllm_action_exec_eval.json
```

如果只想先测前 10 条：

```bash
python3 scripts/evaluate_sql_execution_from_report.py \
  --report-path output/eval_reports/vllm_action_eval.json \
  --output-path output/eval_reports/vllm_action_exec_eval_smoke.json \
  --max-samples 10
```

## 9. 结果怎么看

重点看输出 JSON 里的这些指标：

- `pred_exec_success_rate`
  预测 SQL 的可执行率

- `result_match_rate`
  预测 SQL 的执行结果是否和 gold SQL 一致

- `action_type_accuracy`
  `<sql>` / `<solution>` 动作类型是否选对

你也可以直接打开输出文件看具体样本：

- `pred_sql`
- `gold_sql`
- `pred_observation`
- `gold_observation`
- `result_match`

## 10. 常见问题

### 10.1 远端连不上 `127.0.0.1:19030`

优先检查：

1. 本地 StarRocks 是否真的启动
2. SSH 反向隧道会话是否还活着
3. 远端命令里的端口是不是 `19030`

本地先自检：

```bash
docker exec -it quickstart mysql -P 9030 -h 127.0.0.1 -u root -e "SHOW DATABASES;"
```

### 10.2 远端可以连上，但脚本执行报库不存在

检查远端环境变量：

```bash
echo $DB_HOST
echo $DB_PORT
echo $DB_NAME
```

确认是：

```bash
DB_HOST=127.0.0.1
DB_PORT=19030
DB_NAME=TGAC
```

### 10.3 SSH 一断，远端数据库就不可用了

这是正常现象，因为数据库访问依赖 SSH 隧道。

建议：

- 使用 `tmux`
- 或使用 `autossh`

## 11. 更稳的方式

如果你后面长期要在远端做：

- SQL 执行评测
- Dataflow 数据合成
- RL rollout

那更稳的做法是：

- 直接在一台固定机器上部署 StarRocks
- 或把本地数据迁到远端数据库服务

但在当前这个阶段，**SSH 反向隧道已经足够用**。

## 12. 最小可用流程

### 本地

```bash
docker start quickstart
ssh -N -R 19030:127.0.0.1:9030 your_user@your_remote_server
```

### 远端

```bash
export DB_HOST="127.0.0.1"
export DB_PORT="19030"
export DB_USER="root"
export DB_PASSWORD=""
export DB_NAME="TGAC"
```

然后：

```bash
python3 scripts/evaluate_sql_execution_from_report.py \
  --report-path output/eval_reports/vllm_action_eval.json \
  --output-path output/eval_reports/vllm_action_exec_eval.json
```
