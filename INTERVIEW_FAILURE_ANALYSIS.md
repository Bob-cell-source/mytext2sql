# Text2SQL 项目失败样本分析与改进方案

这份文档用于回答面试中常见的几个问题：

- 这个 Text2SQL 项目为什么有一部分题没有做出来？
- 没做出来的题，主要难在哪里？
- 这些失败说明了什么技术瓶颈？
- 如果继续做，应该怎么系统性改善？
- 面试时应该如何把这些问题讲清楚，而不是显得项目“不完整”？

本文基于以下项目文件做分析：

- 原始题库：[final_dataset.json](/root/text2sql_RL/final_dataset.json)
- 已确认可作为 gold 的题库：[golden_sql_marked.json](/root/text2sql_RL/golden_sql_marked.json)
- 当前 SQL 执行结果：[dataset_exe_result.json](/root/text2sql_RL/dataset_exe_result.json)
- 结果对比脚本：[find.py](/root/text2sql_RL/find.py)
- 正确题目 ID 维护脚本：[right.py](/root/text2sql_RL/right.py)

## 1. 先说结论

这个项目里“没有做出来的题”不能简单理解成“模型不会写 SQL”。

更准确地说，失败分成三层：

1. SQL 根本执行不了
2. SQL 能执行，但结果为空
3. SQL 能执行，但结果口径和标准答案不一致

这三类问题的技术含义完全不同。

从当前数据来看：

- 总题数：101
- 已进入 `golden_sql_marked.json` 的题数：72
- 未进入 gold 的题数：29

这 29 题进一步拆分：

- 执行报错：1 题
- 执行为空：2 题
- 执行成功但未纳入 gold：26 题

也就是说，真正“完全跑不起来”的题非常少，大部分问题出在“业务口径没有对齐标准答案”。

这是 Text2SQL 项目里非常典型也非常重要的结论：  
**难点主要不在 SQL 语法生成，而在业务语义到查询口径的精确映射。**

## 2. 数据结论

### 2.1 样本数量

- `final_dataset.json`：101 题
- `golden_sql_marked.json`：72 题
- 差集：29 题

未进入 gold 的 29 题如下：

`sql_8, sql_10, sql_19, sql_20, sql_29, sql_44, sql_52, sql_53, sql_54, sql_55, sql_58, sql_61, sql_62, sql_80, sql_84, sql_85, sql_88, sql_89, sql_90, sql_91, sql_93, sql_98, sql_99, sql_100, sql_108, sql_110, sql_115, sql_117, sql_120`

### 2.2 复杂度分布

已做成 gold 的 72 题复杂度分布：

- 简单：19
- 中等：36
- 复杂：17

未做成 gold 的 29 题复杂度分布：

- 简单：1
- 中等：7
- 复杂：21

这说明未解决样本明显集中在高复杂度题上，不是随机遗漏。

### 2.3 执行状态分布

未做成 gold 的 29 题执行状态：

- `success`：26
- `empty`：2
- `error`：1

这组数据很关键。它说明项目的瓶颈已经不是基础的 SQL 生成能力，而是“生成后是否真正符合业务定义并对齐标准结果”。

## 3. “失败”到底是什么意思

### 3.1 执行失败

这是最直观的一类失败。

含义：

- SQL 语法错误
- 引用字段错误
- join 逻辑导致数据库报错
- 函数或类型不兼容

典型样本：

- `sql_88`

这类题通常说明模型在 schema 使用、表达式拼装或查询结构上有明显问题。

### 3.2 执行为空

这类题更隐蔽。

SQL 本身能跑，但因为：

- 时间范围写错
- 过滤条件过严
- 玩法映射错
- join 条件不对

最终结果变成空集。

典型样本：

- `sql_54`
- `sql_84`

这类失败不一定是“数据库里没数据”，更常见的是“业务口径理解偏了，导致用户集合或事件集合被筛没了”。

### 3.3 执行成功但结果不匹配

这是最难、也是最有价值的一类失败。

它意味着：

- SQL 形式上合理
- 能执行
- 还能返回看起来“像答案”的结果
- 但和标准答案不一致

这种情况在 Text2SQL 里最常见，也最接近真实业务难题。

典型原因包括：

- 时间窗口定义错
- 回流/留存口径错
- 用户集合定义错
- 多张表的主键映射错
- 玩法分类规则错
- 分层区间边界错
- 应该按天算再按周聚合，但直接整体聚合了

这类题占了这次失败样本的大多数。

## 4. 失败原因的核心分类

从这 29 题看，失败原因可以归纳成四类。

### 4.1 业务口径复杂，时间窗口和用户集合难定义

这是最大头的问题。

很多题不是简单的“统计某段时间内多少人”，而是涉及：

- 新增用户
- 回流用户
- 持续活跃
- 7 留、14 留、30 留
- 周流失、双周流失、月流失
- 盘内 / 盘外
- 近 30 天低活跃
- 上周没参与、下周参与

这些题的难点是集合运算和时间窗口边界，而不是 SQL 关键字。

典型题目：

- `sql_53`
- `sql_80`
- `sql_90`
- `sql_91`
- `sql_108`
- `sql_110`
- `sql_117`
- `sql_120`

这类题的典型风险是：

- “当天活跃”与“过去 N 天未活跃”的逻辑写反
- 新注册用户没有排除
- 留存定义用了登录口径，但题目实际上要玩法口径
- 该按自然周统计，却按滚动 7 天统计

一句话总结：  
**时间窗口类题目的难点是业务集合定义，不是 SQL 语法。**

### 4.2 玩法分类复杂，不能直接按字段值 group by

很多题里提到的“玩法”“子玩法”“垂类”“主题群屿”“对决冠军”“周边休闲模式”等，并不是数据库里天然有一个标准字段能直接用。

通常需要：

- `modename + submode + mapname` 联合判断
- 多种玩法名称映射到统一业务分类
- 特殊玩法要单独识别
- 分类后再做统计

典型题目：

- `sql_19`
- `sql_54`
- `sql_55`
- `sql_80`
- `sql_88`
- `sql_89`

这类题的常见问题：

- 模型能生成一个看起来合理的 `CASE WHEN`
- 但业务映射和官方定义不完全一致
- SQL 跑得通，但结果和标准答案偏差很大

一句话总结：  
**玩法类题的难点是业务映射规则隐含在题意里，而不是显式写在 schema 里。**

### 4.3 多表关联、多种 ID 体系，join 错了也能跑通

这是非常典型的工业级 Text2SQL 难点。

一个题里可能同时出现：

- 登录表
- 对局表
- 消费表
- 赛季维表
- 角色映射表
- 用户属性表

同时，用户标识可能不是统一的：

- `iuserid`
- `vplayerid`
- `vroleid`
- `subject`

这会带来两个风险：

1. 模型不知道应该用哪个 ID 关联
2. 即便关联错了，SQL 也可能能执行并返回结果

典型题目：

- `sql_10`
- `sql_20`
- `sql_61`
- `sql_62`
- `sql_99`
- `sql_100`

例如有些 SQL 注释里已经暴露这个问题：把两个不同 ID 体系“假定为同一用户”来 join，这种做法很容易算偏。

一句话总结：  
**多表题不只是 SQL 更长，而是 schema alignment 更难。**

### 4.4 排名、榜单、峰值、滑窗统计复杂

这一类题看起来像普通聚合，但实质上要求较复杂的统计过程：

- 每天都要出榜
- 按自然周统计
- 计算峰值时间
- 统计后 20%
- 做 7 天平均、7 天滑窗
- 先日级计算再周级聚合

典型题目：

- `sql_8`
- `sql_29`
- `sql_52`
- `sql_55`
- `sql_58`
- `sql_115`

这类题的常见问题：

- 少做了一层聚合
- 时间窗口没展开
- 排名是在错误粒度上算的
- 把“每天一个榜单”写成“整个周期一个榜单”

一句话总结：  
**复杂聚合题的难点在统计过程设计，而不是 select 语句本身。**

## 5. 代表性失败样本怎么讲

面试时不需要把 29 题逐条背下来，但最好能举几个代表例子。

### 5.1 `sql_88`：执行报错

这是最标准的执行失败。

说明模型在复杂玩法分类和多段 CTE 组合时，结构稳定性还不够。

面试时可以讲：

> 这类失败是最容易识别和修复的，因为数据库会直接报错。它说明问题在 SQL 结构层，而不是业务口径层。

### 5.2 `sql_54`：能执行但返回空结果

这题的核心问题不在 SQL 语法，而在逻辑路径错了。

题目要求的是：

- `matchmode = 1003`
- 每种玩法
- 周期内日均参与人数
- 日均玩法次留

但现有 SQL 更像是在拿“日期”去套“玩法上线周期”，没有把玩家真实参与的玩法集合严谨建出来，因此最终容易算空。

面试时可以讲：

> 这种题说明模型已经能组织复杂 SQL 结构了，但中间集合定义错了，一旦参与集合错了，后面指标再怎么聚合都没意义。

### 5.3 `sql_84`：时间口径不完整导致空结果或失真

题目要求：

- 2018 年注册
- 近 30 天活跃
- 在 2020.09 到 2022.04 添加过好友
- 统计每个月人均在线好友数

而现有 SQL 的统计窗口更偏向“近 30 天登录表”。

这说明模型把“筛选用户条件”和“最终统计时间范围”混在一起了。

面试时可以讲：

> 这类题最容易犯的错误是把 eligibility window 和 measurement window 混成一个窗口。前者是筛选用户，后者是实际统计指标，两者不一定是同一个时间区间。

### 5.4 `sql_19`：结果看起来有输出，但业务可信度低

这题执行成功，但 SQL 里把“周活跃频次”直接写成常数 `7.0`。

这类问题很值得拿出来讲，因为它体现出：

- SQL 不是越能执行越好
- 业务指标必须从数据推导，不能拍脑袋补常数

面试时可以讲：

> 我们在项目里后面意识到，执行通过不等于业务正确。像这种常数字段就是一个很典型的假阳性，需要结果校验来兜住。

### 5.5 `sql_20`：ID 体系风险

这题涉及网吧特权、对局、登录、付费等多个表。

现有 SQL 注释里有一个明显风险：

- 把 `vroleid` 和 `iuserid` 直接视作同一对象

这类 SQL 很危险，因为：

- 能执行
- 数值也可能看起来正常
- 但统计主体已经错了

面试时可以讲：

> 在真实数仓里，最难的不是写 join，而是确认 join key 对不对。这个项目里很多复杂题失败，本质上就是 schema 对齐问题。

### 5.6 `sql_80`：周期题没有真正展开

题目要求“每 7 天为一个周期”，但最终结果只有 1 行。

这通常意味着：

- 周期生成逻辑不完整
- 最终聚合粒度塌缩了

面试时可以讲：

> 复杂统计题很容易出现‘看起来 SQL 很长，但最后少了一层 group by 或周期展开’，导致结果粒度不对。

## 6. 什么样的题是“比较难的题”

这个问题面试官很可能单独追问。

可以用下面这个框架回答。

### 6.1 我对难题的定义

我会把以下特征作为 Text2SQL 难题的判断标准：

1. 不是单表题，而是 3 张以上表联合分析
2. 用户 ID 体系不统一，需要中间映射
3. 有滑窗、自然周、跨期对比、留存回流定义
4. 有业务术语，需要口径解释而不是字面翻译
5. 玩法分类需要人工规则映射
6. 结果不是简单计数，而是复合指标
7. 题目隐含很多默认条件，没有完全结构化写出

### 6.2 为什么这些题难

因为它们同时包含三类复杂性：

- **语义复杂性**：题意里有很多隐含业务规则
- **结构复杂性**：SQL 需要多层 CTE、多次聚合、多次 join
- **验证复杂性**：就算 SQL 跑通，也不容易一眼判断是不是对

### 6.3 一句话概括

> 越依赖业务口径、集合边界和时间窗口定义的题越难，越接近真实 BI 报表口径的题越难。

## 7. 这些失败说明了什么技术瓶颈

从工程角度看，这批失败说明了以下几个核心瓶颈。

### 7.1 Text2SQL 的主要瓶颈不是语法，而是口径对齐

项目早期很容易把问题理解成：

- schema 没喂够
- prompt 不够长
- SQL 模板不够全

但从这 29 题看，真正的问题不是这些。

因为大多数失败样本：

- SQL 能写出来
- 还能执行
- 只是答案不对

所以主要瓶颈其实是：

- 业务术语理解
- 中间集合构造
- 时间窗口定义
- 指标口径一致性

### 7.2 “可执行”不是一个足够好的优化目标

如果只奖励模型“生成一个可执行 SQL”，模型会学到很多假阳性策略：

- 过滤过严导致空结果
- 少做几层逻辑，先给一个看起来合理的结果
- 用近似条件代替严格条件

这在离线检查时很危险，因为：

- SQL 看起来没问题
- 输出还有数据
- 但业务上是错的

### 7.3 缺少业务口径结构化资产

很多题的难点其实不是数据库结构，而是业务定义没有结构化沉淀。

例如：

- 什么叫 7 日回流
- 什么叫玩法次留
- 什么叫垂类独占
- 什么叫周流失 / 双周流失 / 月流失

如果这些规则只存在于自然语言题目里，模型每次都需要临场推断，稳定性会很差。

### 7.4 缺少复杂题的中间态校验

很多失败题的共同问题是：

- 最终 SQL 很长
- 但中间步骤没有显式验证

如果没有把“用户集合”“玩法映射”“日级明细”“周期窗口”这些中间结果拆开检查，就很难发现是哪一步偏了。

## 8. 对没有做出来的题，应该怎么办

这一段是面试里最重要的部分。

不要把回答停留在“继续调 prompt”或者“换更强模型”。

更好的说法是：  
**要把失败问题工程化拆解，而不是把它们都归因于模型能力不足。**

### 8.1 第一步：按失败类型分桶

先分成三桶：

1. 执行错误桶
2. 空结果桶
3. 结果不匹配桶

这样做的意义是：

- 执行错误优先查 schema 和 SQL 结构
- 空结果优先查过滤条件和 join
- 结果不匹配优先查业务口径和聚合粒度

这是最基本的诊断流程。

### 8.2 第二步：复杂题必须拆成中间步骤

对于复杂题，不应该让模型直接一次输出终版 SQL。

应该拆成以下中间步骤：

1. 确认用哪些表
2. 确认用户主体是谁
3. 确认时间窗口
4. 构造候选用户集合
5. 构造玩法或事件集合
6. 先做日级或周级明细
7. 最后再做汇总聚合

这样每一步都可以单独检查。

例如对“回流用户 + 玩法参与 + 周期统计”类题：

- 第一步先算回流用户名单
- 第二步检查人数是否合理
- 第三步再关联玩法参与明细
- 第四步再做周期聚合

这会比一步到位直接生成长 SQL 稳定很多。

### 8.3 第三步：把业务口径沉淀成模板

针对高频难点，要建立标准模板，而不是每题临场推断。

比如：

- 7 日回流模板
- 次留模板
- 7 留模板
- 自然周模板
- 玩法排名模板
- 付费分层模板

示例：

- 7 日回流：当日活跃，前 7 天不活跃，且不是新注册
- 次留：D 日参与该玩法的用户，在 D+1 日仍参与该玩法的比例
- 自然周：以周一作为起始，而不是简单滚动 7 天

这样做的价值是把“业务语义”前置固化。

### 8.4 第四步：显式维护玩法映射规则

玩法题失败率高，根本原因是分类规则隐式存在。

应该单独沉淀：

- 玩法分类字典
- `modename/submode/mapname` 到业务玩法的映射规则
- 特殊玩法识别规则

不要把这件事完全交给模型临场写 `CASE WHEN`。

### 8.5 第五步：建立 ID 对齐检查机制

复杂题里应当把“主用户 ID 是什么”单独作为一个检查项。

建议每道多表题都回答三个问题：

1. 主统计主体是谁
2. 各张表里对应的字段是什么
3. 有没有映射表支持这个 join

如果这三件事不明确，SQL 能跑也不应该直接信。

### 8.6 第六步：优化 reward 和评测目标

如果后续继续做 RL 或 agent 化训练，reward 不能只看“能不能执行”。

更合理的 reward 分层是：

1. 格式奖励
2. SQL 可执行奖励
3. 中间 probe 有效性奖励
4. 最终结果匹配奖励
5. 对空结果、无效近似、明显偷懒 SQL 做惩罚

也就是说：

**可执行只是基础门槛，结果对齐才是核心目标。**

### 8.7 第七步：人工校验要放在高风险题上

不是所有题都值得投入同样的人力。

更合理的策略是优先人工复核以下题型：

- 留存/回流/流失
- 玩法映射
- 多 ID join
- 排名与滑窗统计
- 多层指标复合题

因为这些题最容易出现“执行正常但语义错误”的假阳性。

## 9. 一个更像工业项目的改进路线

如果面试官追问“你下一步会怎么做”，建议按下面四层来回答。

### 9.1 第一层：诊断层

目标是知道失败在哪一步。

要补的能力：

- 自动按失败类型分桶
- 输出 SQL 错误信息摘要
- 自动识别空结果
- 自动比对结果集
- 保存中间执行统计

### 9.2 第二层：知识层

目标是把业务知识结构化。

要补的资产：

- 留存/回流口径模板
- 玩法映射规则库
- 常见指标定义库
- 主键 / 外键 / 映射关系说明

### 9.3 第三层：生成层

目标是让模型不要一次拍出一个长 SQL，而是有规划地生成。

可以做的事：

- 先生成 plan
- 再生成中间 SQL
- 最后生成终版 SQL
- 对中间步骤做 observation 和修正

### 9.4 第四层：训练层

目标是让模型学会高风险题的稳定做法。

可以做的事：

- 补更多复杂题 gold 样本
- 针对失败高发题型做 targeted SFT
- 在 RL 中提高结果匹配奖励权重
- 对假阳性 SQL 加负反馈

## 10. 面试时怎么讲，最容易显得靠谱

这里给一个推荐表述。

### 10.1 简洁版

> 这个项目一共 101 道题，最后沉淀成 72 条 gold。剩下 29 条里面，真正执行失败的很少，大部分是 SQL 能跑，但结果没有对上标准答案。这个现象让我比较明确地认识到，Text2SQL 的主要难点不是 SQL 语法生成，而是业务口径对齐，尤其是回流留存、玩法分类、多表 ID 映射和滑窗统计这几类题。后续如果继续做，我不会只调 prompt，而是会把失败工程化拆解，先按错误类型分桶，再把高频口径模板化，把复杂题拆成中间步骤做验证，最后再用结果匹配而不是可执行性去驱动训练优化。 

### 10.2 展开版

> 我后来复盘这个项目时发现，一个很重要的认知变化是：Text2SQL 的瓶颈不在“写不写得出 SQL”，而在“写出来的 SQL 是否真的符合业务定义”。从数据上看，未进入 gold 的题里绝大多数 SQL 都是能执行的，但因为时间窗口、用户集合、玩法映射或者 join key 理解偏差，结果和标准答案不一致。也就是说，这不是简单的语法生成问题，而是业务语义映射问题。  
> 所以后续的改进方向不能只靠更长 prompt 或更强模型，而应该补业务口径模板、玩法映射规则、ID 对齐检查和中间步骤验证，把复杂题从“一次生成”改成“分步生成 + 分步校验”。

## 11. 面试官可能继续追问的问题

### 11.1 这些失败说明项目是不是不完整

不建议回答“是”。

更好的回答是：

> 这个项目已经把基础链路打通了，包括数据、生成、执行、比对和 gold 沉淀。但复杂题失败样本暴露了项目从“能做”走向“做稳”时的关键瓶颈，这也是我认为最有价值的部分，因为它说明了系统下一步应该往哪补。

### 11.2 为什么不直接靠大模型解决

可以回答：

> 大模型可以解决通用 SQL 生成，但复杂业务题里的回流、留存、玩法映射、ID 对齐这些问题，本质上需要结构化业务知识和验证闭环。只靠模型参数本身，稳定性是不够的。

### 11.3 如果让你继续做一个月，你最优先做什么

推荐回答：

1. 把 29 道失败题按类型分桶
2. 对留存/回流/玩法类题建立口径模板
3. 给复杂题加中间态执行验证
4. 优先补高复杂度题的 gold 样本
5. 调整 reward，让结果对齐比可执行性更重要

## 12. 你可以向面试官强调的价值点

这部分很重要，因为它决定对方怎么评价你。

建议强调下面三点：

### 12.1 我不是只会报成功率

我能把失败样本拆成：

- 执行失败
- 空结果
- 结果口径不匹配

这说明你会做诊断，而不是只会报指标。

### 12.2 我知道真实 Text2SQL 难点在哪

不是“不会写 SQL”，而是：

- 业务语义难
- 时间窗口难
- 多表对齐难
- 验证难

这会让面试官觉得你理解的是工业问题，而不是玩具任务。

### 12.3 我知道怎么改，不是只会换模型

如果你的改进方案只剩下：

- 换更大模型
- 多调 prompt
- 多训一点数据

那说服力会比较弱。

更强的回答是：

- 模板化业务口径
- 规则化玩法映射
- 显式做 ID 对齐
- 分步生成和校验
- 用结果匹配驱动训练

## 13. 最后一段可直接背

> 这个项目最有价值的地方，不只是做出了 72 条 gold 样本，而是通过剩下 29 条失败样本让我更清楚地看到 Text2SQL 的真实瓶颈。大多数失败不是 SQL 写不出来，而是 SQL 能跑但业务口径不对，尤其集中在回流留存、玩法映射、多表 ID 对齐和滑窗统计这几类高复杂度题上。  
> 所以如果继续做，我不会把重点放在单纯调 prompt，而会把失败系统化拆解：先分桶定位，再把业务口径模板化、玩法映射规则化、复杂题拆成中间步骤验证，最后让训练目标从“可执行”升级为“结果正确且口径一致”。我觉得这是把一个 Text2SQL demo 做成更接近工业可用系统的关键一步。

## 14. 三分钟口述稿

下面这段可以直接按口语方式讲给面试官听。

> 我这个 Text2SQL 项目里，一共有 101 道题，最后沉淀成了 72 条 gold 样本，剩下 29 条没有进入 gold。  
> 我后面专门复盘过这 29 条，发现一个很重要的结论：大多数失败并不是模型不会写 SQL，而是 SQL 能执行，但结果和标准答案对不上。具体来看，29 条里只有 1 条是执行报错，2 条是执行为空，剩下 26 条其实都能跑通。  
> 这说明项目的瓶颈已经不是基础 SQL 生成，而是业务口径对齐。尤其是几类题特别难：第一类是回流、留存、流失这种时间窗口题，难点在用户集合定义；第二类是玩法分类题，题目里的玩法名称往往不是表里一个字段能直接拿来 group by，需要做业务映射；第三类是多表 join 题，很多表的用户 ID 体系并不统一，join key 选错了 SQL 也能跑，但结果是错的；第四类是滑窗、排名、峰值这类复杂统计题，容易在聚合粒度上出错。  
> 所以后来我对这个项目的理解有个变化：Text2SQL 真正难的不是语法，而是把自然语言里的业务定义精确翻译成查询口径。  
> 如果继续做，我不会只调 prompt 或者直接换更大模型。我会先把失败样本按类型分桶，再把高频业务口径模板化，比如回流定义、次留定义、自然周定义；同时把玩法映射和 ID 对齐规则单独沉淀出来。对于复杂题，我会改成分步生成和分步验证，而不是一次生成一个超长 SQL。训练目标上，也不能只奖励“能执行”，而应该把“结果正确、口径一致”作为核心优化目标。  
> 我觉得这次失败样本分析反而是项目里最有价值的部分，因为它让我看清了一个 Text2SQL 系统从能跑 demo 到更接近工业可用之间，真正缺的是什么。

## 15. 29 题逐题失败原因表

下面这张表不是严格的官方错误归因，而是基于题目特征、执行状态和现有 SQL 的工程化归纳。  
面试时不需要逐题背诵，但可以在被追问时快速举例。

| SQL ID | 状态 | 主要失败类型 | 主要原因概括 |
| --- | --- | --- | --- |
| `sql_8` | success | 峰值/排名口径偏差 | 题目要求按月输出服务器节点峰值和多类对局数，峰值时间与总结束数的聚合粒度容易不一致，`MAX_BY` 取峰值时刻的口径也可能和标准答案不一致。 |
| `sql_10` | success | 多表 join + 赛季映射 | 涉及购买表、使用表、主表、赛季表，多种 ID 体系和日期映射并存，很容易出现购买人数与使用人数按错赛季或 join key 对不齐。 |
| `sql_19` | success | 玩法口径 + 指标伪造风险 | SQL 虽能执行，但“周活跃频次”直接写成常数，说明结果不是完整由数据推导，属于可执行但业务可信度不足。 |
| `sql_20` | success | 多 ID 体系 + 复合指标 | 同时统计网吧时长、排位、付费、ARPA、ARPPA 等，且 `vroleid/iuserid` 对齐风险高，任一环节偏差都会导致整体口径错。 |
| `sql_29` | success | 自然周 + 先日后周聚合 | 题目要求周平均日均参与用户数、参与率、参与场次，若没有严格按“先日级再周级”计算，结果会和标准答案偏离。 |
| `sql_44` | success | 分层边界口径 | 付费区间看似简单，但分层边界、小R/中R/大R/超R 的闭开区间和“未知”处理很容易和标准答案不一致。 |
| `sql_52` | success | Top/后 20% + 分布统计 | 先要确定“参与率后 20% 子玩法”，再统计这些用户每天参与所有子玩法数量分布，属于两阶段筛选，容易在排名对象和统计对象上混淆。 |
| `sql_53` | success | 回流分层口径 | 题目要求“非新注册活跃用户、上一周没活跃”的回流用户，并按更早窗口分层，时间窗口多且互斥关系复杂。 |
| `sql_54` | empty | 玩法集合构造错误 | 题目要的是 `matchmode=1003` 下每种玩法的日均参与人数和次留，但现有 SQL 更偏上线周期覆盖，未严谨构造真实玩法参与集合，导致空结果。 |
| `sql_55` | success | 每日榜单 + 七天平均 | 要求每天提供一个榜单，还要按近 7 天平均参与率、次留、七留综合排名，极易因为滑窗、排名粒度或留存定义不严导致失配。 |
| `sql_58` | success | 多时段玩法分布 | 不同玩法 ID 对应不同历史时段，再统计对局时长人数分布，容易把时间段、玩法 ID、分桶区间三者的对应关系写错。 |
| `sql_61` | success | 付费分层 + 历史窗口 | 涉及首日累充、历史付费、前一个月点券消耗、沉默付费定义，典型的业务口径复合题。 |
| `sql_62` | success | 角色粒度 + 多表指标 | 题目强调按 UID 还是角色计算会直接影响结果；联盟参与、任务完成、商店兑换等指标来自不同表，粒度一致性难。 |
| `sql_80` | success | 跨周行为迁移 | 题目要求按每 7 天周期比较“上周没参与、下周参与”的用户行为，当前结果只出 1 行，说明周期展开或最终聚合粒度很可能有问题。 |
| `sql_84` | empty | 筛选窗口与统计窗口混淆 | 题目先按注册年、近 30 天活跃、历史加好友筛用户，再统计每个月在线好友数；现有 SQL 更像只在近 30 天窗口里聚合，时间口径不完整。 |
| `sql_85` | success | 双区间对比口径 | 题目同时看两个日期区间，统计活跃、匹配、微赛事参与情况，容易在日期拼接、是否去重和区间独立统计上出错。 |
| `sql_88` | error | SQL 结构失败 | 多层 CTE、垂类玩法分类、周分布与段位变化同时建模，结构复杂度高，最终直接执行报错。 |
| `sql_89` | success | 周期分布 + 活跃度分层 | 题目要求赛季末期垂类玩法轻中重度及独占用户的周登录天数、参与局数和天数分布，涉及多层分类和多维分布，容易错粒度。 |
| `sql_90` | success | 活跃分层 + 后续行为 | 要先按截至某日的活跃分层，再排除中间窗口不活跃用户，再看后续是否玩桥梁争夺，多个阶段的集合运算很容易写偏。 |
| `sql_91` | success | 回流类型留存 | 要看两段时间全平台活跃用户的回流情况，并输出 7/14/30/60/90/180 留，属于典型的留存口径题。 |
| `sql_93` | success | 新增/持续/回流付费 | 题目要求按月区分新增付费、持续付费、回流付费，关键难点是付费用户状态转移定义而不是金额求和。 |
| `sql_98` | success | 高活跃用户定义 | 要统计乐园高活和砺刃高活用户及其登录人数，核心在“高活跃”的业务阈值定义和不重复口径。 |
| `sql_99` | success | 付费分层 + 子玩法维度 | 同时按乐园子玩法、砺刃/乐园分类、付费分层来统计人数，容易在分层金额来源和子玩法归属上失配。 |
| `sql_100` | success | 复杂商业化归因 | 玩家类型、总人数、总付费、付费人数、商业化类型、抽奖/购买次数等都要同时输出，是多表、多类型事件归因题。 |
| `sql_108` | success | 多种流失定义 | 周流失、双周流失、月流失和近 30 天登录少于 3 天低活跃，需要多套规则并列输出，逻辑边界稍有偏差就错。 |
| `sql_110` | success | 登录留存 vs 活动留存 | 同时要求登录和活动两套次留/3留/7留，若没有把主体事件区分清楚，很容易把两类留存混算。 |
| `sql_115` | success | 多赛事多周期汇总 | 多个竞赛杯在不同日期段，最终统一统计，容易在赛事标签映射、日期集合并和周期聚合上出错。 |
| `sql_117` | success | 回流类型判定 | 题目统计当天活跃前 30 用户的回流类型，重点在“活跃/7 日回流/14 日回流/30 日回流”的互斥定义。 |
| `sql_120` | success | 持续活跃/流失到盘外口径 | 题目同时包含持续活跃和盘外流失的状态描述，现有 SQL 只输出一个固定标签，语义覆盖明显不足。 |

## 16. 面试前的使用建议

如果你要拿这份文档去准备面试，建议这样用：

1. 先看第 1、2、4、8、10、13、14 节
2. 这几节足够支撑大多数面试回答
3. 如果面试官追问具体失败例子，再翻第 5 节和第 15 节
4. 如果面试官问“接下来怎么改”，重点讲第 8、9 节

最推荐你记住的不是每一道题，而是下面这条主线：

> 大多数失败不是 SQL 语法失败，而是业务口径失败；真正的改进方向不是只调模型，而是把业务口径、玩法映射、ID 对齐和中间验证工程化。

## 17. 这个项目涉及到哪些后端知识

虽然这个项目表面上是一个 Text2SQL + SFT/RL 项目，但如果从工程视角看，它已经涉及了不少典型的后端知识。

### 17.1 离线任务编排与数据 pipeline

项目当前是明显的 script-first 结构，多个脚本串起了完整流程：

- 数据准备
- SQL 生成
- SQL 执行
- 结果比对
- SFT/RL 数据导出
- 训练与评测

相关文件包括：

- [final_code.py](/root/text2sql_RL/final_code.py)
- [generate_sft_dpo_data.py](/root/text2sql_RL/generate_sft_dpo_data.py)
- [scripts/prepare_grpo_dataset.py](/root/text2sql_RL/scripts/prepare_grpo_dataset.py)
- [scripts/evaluate_sql_execution_from_report.py](/root/text2sql_RL/scripts/evaluate_sql_execution_from_report.py)

这本质上就是一个离线数据 pipeline，只是还没有上 Airflow、Argo 或定时调度器。

### 17.2 数据访问层与 SQL 执行层

项目里有明确的 SQL 执行模块：

- [sql_exe.py](/root/text2sql_RL/sql_exe.py)
- [agent_rl/sql_env.py](/root/text2sql_RL/agent_rl/sql_env.py)

这里涉及典型的后端数据访问问题：

- 数据库连接管理
- 查询执行
- 错误处理
- 结果回收
- 结果标准化
- 执行环境隔离

从后端视角看，这已经不是“写一条 SQL”那么简单，而是在实现一个受控的 query execution layer。

### 17.3 配置管理与环境隔离

项目里已经依赖较多环境变量和外部配置，例如：

- 模型 API Key
- 模型名
- 数据库地址
- 账号密码
- 输出目录

这属于后端里非常常见的配置治理问题：

- 如何区分开发环境、训练环境、评测环境
- 如何避免敏感配置硬编码
- 如何让不同脚本共享统一配置

### 17.4 状态持久化与结果追踪

当前项目大量使用 JSON 文件保存中间产物和最终产物：

- [golden_sql_marked.json](/root/text2sql_RL/golden_sql_marked.json)
- [dataset_exe_result.json](/root/text2sql_RL/dataset_exe_result.json)
- `prepare_report.json`
- 各种训练数据和 rollout 数据

这说明项目已经涉及到轻量级的状态持久化问题。  
从后端角度，它对应的是：

- 任务表
- 执行记录表
- 评测结果表
- 数据版本管理

### 17.5 结果校验与质量评估

项目里不只是生成 SQL，还在做结果比对和质量验证：

- [find.py](/root/text2sql_RL/find.py)
- [right.py](/root/text2sql_RL/right.py)
- [scripts/analyze_sql_exec_failures.py](/root/text2sql_RL/scripts/analyze_sql_exec_failures.py)

这类能力在后端里对应：

- 回归验证
- 输出验收
- 质量监控
- 自动化诊断

### 17.6 Agent 执行框架与工作流设计

`agent_rl` 目录本质上已经在做一个有状态执行系统：

- [agent_rl/rl_env.py](/root/text2sql_RL/agent_rl/rl_env.py)
- [agent_rl/grpo_runner.py](/root/text2sql_RL/agent_rl/grpo_runner.py)
- [agent_rl/teacher_rollout.py](/root/text2sql_RL/agent_rl/teacher_rollout.py)
- [agent_rl/online_rollout.py](/root/text2sql_RL/agent_rl/online_rollout.py)

这里面有很典型的后端工作流概念：

- action
- observation
- 状态转移
- reward
- rollout
- 失败重试

从工程视角看，这和策略引擎、工作流引擎、任务执行引擎是很接近的。

### 17.7 日志、报告和可观测性

项目里有日志和大量 report 产出：

- 执行结果
- prepare report
- failure analysis
- evaluation report

这其实已经在解决后端里的 observability 问题，只是目前更偏实验分析，而不是正式线上服务的日志系统。

## 18. 从后端工程角度，有什么可以优化的地方

如果面试官问“除了模型本身，还有哪些工程优化空间”，这一节可以直接回答。

### 18.1 从脚本集合升级为分层结构

当前项目更像研究代码，入口脚本都在 repo root。

这适合快速迭代，但长期会遇到几个问题：

- 逻辑复用差
- 配置分散
- 测试不方便
- 很难演进成服务

更合理的结构是拆层：

- `data/`
- `execution/`
- `evaluation/`
- `training/`
- `agent/`
- `common/`

这样可以把“生成、执行、评测、训练、配置”这些职责拆开。

### 18.2 把 JSON 文件状态管理升级成正式数据模型

现在用 JSON 管理实验资产没有问题，但如果继续扩展，问题会越来越明显：

- 难做增量更新
- 难支持并发
- 难按条件检索
- 难做版本追踪

改进方向可以分两步：

1. 先统一 schema，明确 `task / execution / evaluation / gold_record`
2. 再落到 SQLite 或 MySQL，做轻量数据库管理

这会让项目更像真正的后端系统。

### 18.3 强化 SQL 执行层的治理能力

SQL 执行器是整个项目的高风险组件，建议补的能力包括：

- 查询超时
- 错误码标准化
- 只读保护
- 危险 SQL 校验
- 重试策略
- 执行耗时统计
- 结果大小限制

这部分如果做好，本质上就是一个后端里的“安全 SQL 沙箱”。

### 18.4 统一配置管理

建议把分散在脚本里的配置读取统一起来，例如做一个 `config.py` 或配置模块，集中管理：

- 数据库连接
- 模型配置
- 路径配置
- 实验开关
- 输出目录

这样能避免：

- 多处重复写环境变量读取逻辑
- 不同脚本配置项名字不一致
- 环境切换容易出错

### 18.5 引入统一日志格式和 trace id

当前项目更偏实验输出，不太像服务化日志。

如果要增强后端工程性，建议每次任务都带上标准字段：

- `run_id`
- `sql_id`
- `stage`
- `model_name`
- `status`
- `latency_ms`

这样后续定位失败题、复现问题、统计耗时会快很多。

### 18.6 做缓存和增量执行

这个项目里很多步骤都是重复性的：

- schema 组装
- prompt 构建
- SQL 执行
- 结果归一化
- 结果比对

如果每次都全量重跑，会浪费大量时间。

后端化优化可以考虑：

- 对已执行 SQL 做缓存
- 对已评测样本做缓存
- 支持按 `sql_id` 增量重跑
- 支持按失败样本重跑

### 18.7 补测试体系

当前项目更偏脚本验证，缺少正式测试框架。

优先值得补测试的模块包括：

- [agent_rl/result_matcher.py](/root/text2sql_RL/agent_rl/result_matcher.py)
- [agent_rl/prompt_builder.py](/root/text2sql_RL/agent_rl/prompt_builder.py)
- [agent_rl/reward.py](/root/text2sql_RL/agent_rl/reward.py)
- SQL 结果归一化与对比逻辑

原因很简单：

- 这些模块一旦改动，可能不会直接报错
- 但会 silently 影响最终指标

这类回归问题是典型的后端工程问题。

### 18.8 把失败分析前置成系统能力

这个项目最核心的经验之一是：

- 不是 SQL 不能执行
- 而是 SQL 能执行，但口径错

所以系统层面应该补的不是只有模型，而是“自动诊断能力”，例如：

- 中间结果检查
- 用户集合检查
- 时间窗口检查
- join key 检查
- 空结果预警
- 多层聚合粒度检查

这会极大提升复杂题调试效率。

### 18.9 如果未来服务化，可以加 API 层和任务队列

如果面试官问“这个项目能不能产品化”，一个合理的回答是：

- 用 FastAPI 暴露接口
- 请求进入后先落任务
- 长耗时任务进入队列异步执行
- 前端轮询任务状态
- 最终返回 SQL、执行结果、解释和评测结果

这样整个系统就从实验脚本变成一个标准的后端服务。

## 19. 这部分在面试时怎么说更自然

你可以这么讲：

> 虽然这个项目表面上是一个 Text2SQL 和 RL 项目，但它已经涉及不少后端工程能力，比如离线数据 pipeline、SQL 执行层、配置管理、结果持久化、自动评测、任务工作流和日志分析。  
> 如果继续往工程化推进，我觉得最值得补的不是单纯换模型，而是把它从研究脚本升级成一个更稳定的后端系统，包括分层结构、统一配置、执行治理、缓存增量、测试体系和失败诊断能力。这样项目的说服力会更强，因为它体现的是系统建设能力，而不是只会调模型。

## 20. 知识层改进，也可以理解成语义层建设

前面提到的“知识层改进”，在很多公司内部其实会被直接叫做：

- 语义层
- 指标层
- 业务语义层
- semantic layer

本质上是一件事：  
**把自然语言中的业务定义，转成可复用、可约束、可执行的中间表示，而不是每次都让模型从零猜。**

### 20.1 为什么需要语义层

从这个项目的失败样本看，很多问题并不是 SQL 语法生成失败，而是业务语义没有被结构化，导致模型每次都在临场推断口径。

最典型的错误包括：

- 回流定义错
- 留存口径错
- 玩法映射错
- 分层边界错
- 时间窗口错
- join 主体错

这些问题共同说明：

> Text2SQL 的瓶颈不只是“能不能写出 SQL”，而是“业务定义有没有先被翻译成稳定的语义规则”。

### 20.2 语义层到底在做什么

可以把语义层理解成业务翻译器。

用户问的是：

- 统计回流用户
- 看月付费率
- 统计玩法次留
- 看高校特权激活率

但数据库真正需要的是：

- 使用哪张表
- 用户主体是谁
- 时间窗口怎么定义
- 过滤条件是什么
- 聚合粒度是什么
- 多表 join 路径是什么

语义层的作用，就是把这个中间层补出来。

### 20.3 语义层的常见形态

在工程里，语义层通常会以几种方式出现。

#### 20.3.1 指标定义层

把高频业务概念直接定义成标准规则，例如：

- `7日回流用户 = 当日活跃 AND 前7日不活跃 AND 非新注册`
- `次留 = D日参与用户中 D+1日仍参与同一玩法的比例`
- `ARPU = 总收入 / 活跃用户数`
- `ARPPU = 总收入 / 付费用户数`

这类定义一旦沉淀下来，模型就不需要每次自己“理解回流”。

#### 20.3.2 维度映射层

很多业务分类不是数据库天然字段，而是业务规则映射，例如：

- “主题群屿”对应哪些 `modename/submode/mapname`
- “竞品业务”对应哪些 `sgamecode`
- “垂类玩法”对应哪些玩法 ID

这种映射表，本质上也是语义层的一部分。

#### 20.3.3 模板层

把高频复杂题沉淀成查询模板，例如：

- 留存模板
- 回流模板
- 付费分层模板
- 排名模板
- 周期对比模板

模型不再直接从自然语言一步跳到完整 SQL，而是先判断应该调用哪类模板，再填参数。

#### 20.3.4 函数层 / DSL 层

更工程化的做法，是先把问题转成函数调用或 DSL，再由系统翻译成 SQL。

例如函数式表示：

```text
get_reflow_users(window=7, date='20250724')
get_retention_users(type='次留', event='玩法参与')
bucket_pay_users(ranges=[12,60,180])
```

或者 DSL：

```json
{
  "metric": "reflow_users",
  "window": 7,
  "exclude_new_user": true,
  "date": "20250724"
}
```

这条链路其实是：

> 自然语言 -> 语义中间表示 -> SQL

而不是：

> 自然语言 -> SQL

### 20.4 这类设计对你项目有什么意义

如果把这个项目继续做下去，我认为最值得补的不是继续堆 prompt，而是增加一层语义抽象。

最适合你这个项目的落地方向是三层：

#### 第一层：指标语义词典

先把高频概念沉淀成标准定义：

- 回流
- 留存
- 流失
- 高活跃
- 独占
- 付费分层
- 自然周
- 次留
- 七留

#### 第二层：模板层

把高频难题抽成模板：

- `retention_template`
- `reflow_template`
- `pay_bucket_template`
- `mode_rank_template`
- `weekly_participation_template`

#### 第三层：映射层

单独维护业务映射：

- 玩法名 -> 字段组合
- 业务名 -> 枚举值集合
- 用户类型 -> 位图 / 活跃规则
- 指标主体 -> 表和主键路径

### 20.5 这比直接 Text2SQL 稳在哪里

直接做 `NL -> SQL` 时，模型每次都在猜：

- 回流到底怎么算
- 玩法名映射到哪些字段
- 周窗口怎么切
- 付费分层边界怎么定

一旦补了语义层，模型不再每次重造口径，而是：

1. 先识别业务意图
2. 落到语义规则或模板
3. 再由系统翻译成 SQL

这样做的价值是：

- 稳定性更高
- 结果更可解释
- 更容易排查错误
- 更适合做评测和回归

### 20.6 面试时怎么说

推荐这样表达：

> 我后面复盘这个项目时，觉得单纯做 Text2SQL 还不够，因为很多失败不是语法生成失败，而是业务语义没有结构化，导致模型每次都在重新猜口径。所以一个更稳的方向是补语义层，也可以理解成知识层。它把像回流、留存、玩法分类、付费分层这类业务逻辑抽象成标准定义、模板或者函数式中间表示，再由系统翻译成 SQL。这样模型不再直接从自然语言一步跳到 SQL，而是先落到可约束的语义表示，再做执行层转换，稳定性会高很多。 

### 20.7 一句话概括

> 所谓知识层改进，很多公司会叫语义层建设，本质就是把业务逻辑先结构化成指标定义、模板、映射规则或 DSL，再生成 SQL，而不是让模型每次从自然语言直接硬猜。

## 21. 结构化语义资产应该怎么存，怎么查，怎么演进

如果继续往语义层方向做，一个很现实的问题就是：

> 这些结构化资产会越来越多，到后面怎么保证检索效率和匹配准确率？

这个问题不能只回答“放向量库”或者“存成 JSON 文件”，因为那样随着资产规模增长，很容易出现：

- 资产越来越多，但查不准
- 向量召回太泛
- 相似术语互相污染
- 检索延迟越来越高
- 旧规则和新规则混在一起

所以更合理的做法，是把它当成一个**结构化语义资产库 + 分阶段检索系统**来设计。

### 21.1 先分层，不要把所有资产混成一个大知识库

最少建议拆成四类资产：

#### 第一类：指标定义

例如：

- 回流
- 留存
- 流失
- ARPU
- ARPPU
- 高活跃

这类资产的作用是定义“一个业务概念到底怎么算”。

#### 第二类：映射规则

例如：

- 玩法名 -> 字段组合
- 业务名 -> 枚举值集合
- 用户类型 -> 位图规则

这类资产解决的是“自然语言业务词到底映射到哪些字段和值”。

#### 第三类：模板

例如：

- 留存模板
- 回流模板
- 付费分层模板
- 排名模板
- 周期对比模板

这类资产解决的是“这类题应该用什么查询结构”。

#### 第四类：经验案例

例如：

- 某类题为什么经常错
- 常见坑是什么
- 应该优先检查什么

这类资产更像失败经验和修复策略沉淀。

### 21.2 推荐的存储方式

最稳的第一版做法，不是单独上一个复杂的知识系统，而是：

> 关系型数据库 + JSON 字段

比如 PostgreSQL / MySQL 都可以。

原因很简单：

- 能做结构化过滤
- 能做版本控制
- 能打标签
- 能做质量评分
- 能支持人工审核
- 后续也容易接向量检索

### 21.3 一个通用的资产表可以长什么样

可以抽一张 `semantic_assets` 表，包含如下字段：

- `id`
- `asset_type`
- `name`
- `domain`
- `tags`
- `trigger_terms`
- `structured_payload`
- `version`
- `quality_score`
- `usage_count`
- `success_rate`
- `status`
- `created_at`
- `updated_at`

其中：

- `asset_type` 用来区分 `metric / mapping / template / case`
- `domain` 用来区分 `retention / reflow / payment / ranking / gameplay`
- `structured_payload` 用 JSON 存每类资产的具体定义
- `trigger_terms` 存高频触发词，方便快速过滤

### 21.4 每类资产应该怎么结构化

#### 指标定义示例

```json
{
  "metric_name": "7日回流用户",
  "definition": "当日活跃，前7日不活跃，且非新注册",
  "subject": "user",
  "time_window": {
    "current_day": true,
    "lookback_days": 7
  },
  "filters": [
    "active_today = true",
    "active_in_last_7_days = false",
    "is_new_user = false"
  ]
}
```

#### 映射规则示例

```json
{
  "mapping_name": "竞品业务",
  "field": "sgamecode",
  "values": [
    "initiatived",
    "jordass",
    "esports"
  ]
}
```

#### 模板示例

```json
{
  "template_name": "retention_template",
  "inputs": ["date", "event_type", "window"],
  "steps": [
    "build_day_d_user_set",
    "build_day_d_plus_1_user_set",
    "intersect_users",
    "aggregate_ratio"
  ],
  "sql_skeleton": "WITH base AS (...)"
}
```

#### 经验案例示例

```json
{
  "case_name": "回流题常见错误",
  "failure_pattern": "把 eligibility window 和 measurement window 混淆",
  "fix_strategy": "先筛用户集合，再统计指标窗口",
  "related_templates": ["reflow_template"]
}
```

### 21.5 为什么不能只靠向量库

很多人会第一反应说：“做 embedding 检索就行”。

但在这种业务场景里，只靠向量库不够稳，原因包括：

1. 召回太泛
2. 相似术语容易互相污染
3. 数字边界和时间窗口不稳定
4. 业务定义相近但口径完全不同
5. 可解释性较弱

例如下面这些词 embedding 可能很近，但业务上完全不是一回事：

- 7日回流
- 7留
- 7日活跃

所以正确方向不是“全量向量搜”，而是：

> 规则过滤 + 结构化索引 + 语义检索

### 21.6 推荐的检索流程

一个更稳的检索流程应该是分阶段的。

#### 第一步：粗分类

先判断问题属于哪类：

- 留存 / 回流
- 付费分层
- 排名
- 玩法分类
- 多周期对比

这一步可以用：

- 规则分类
- 小模型分类
- LLM 分类

输出一个 `intent_type`。

#### 第二步：按类型过滤资产

如果问题属于“回流类”，那就先只查：

- `asset_type in (metric, template, case)`
- `domain = reflow`

而不是全库召回。

#### 第三步：按标签和触发词过滤

例如进一步过滤：

- 是否包含“7日”
- 是否包含“玩法”
- 是否包含“自然周”
- 是否包含“付费分层”

这一步能显著缩小候选集。

#### 第四步：语义检索

只在缩小后的候选集上做 embedding 召回。

这样比全量向量检索稳定得多。

#### 第五步：重排序

最后结合多个信号做 rerank：

- 语义相似度
- 历史成功率
- 最近使用频率
- 新鲜度
- 是否人工确认过

一个简单的排序分数可以写成：

```text
final_score =
0.4 * semantic_similarity +
0.2 * success_rate +
0.2 * usage_count_norm +
0.1 * recency_score +
0.1 * human_verified_score
```

### 21.7 怎么避免资产越来越多后失控

除了检索流程，还需要治理机制。

建议每条资产额外维护这些字段：

- `quality_score`
- `usage_count`
- `success_rate`
- `last_used_at`
- `verified_by_human`
- `status`

其中 `status` 可以是：

- `draft`
- `active`
- `deprecated`

基于这些字段，可以做几件事：

#### 冷热分层

- 高频资产放热库
- 低频资产放冷库

#### 版本控制

例如：

- 回流定义 v1 / v2
- 玩法映射 v1 / v2

#### 淘汰机制

- 长期低命中、低成功率的资产降权
- 明显过期的资产标记为 deprecated

### 21.8 对你这个项目，第一版最合适的落地方式

如果现在还不想把系统做太重，我觉得最合适的第一版是：

#### 存储层

- 用 PostgreSQL 或 MySQL 建一张 `semantic_assets` 表
- 每条资产用 JSON payload 存结构化定义
- `tags / trigger_terms / version / verified / success_rate` 单独建列

#### 检索层

按下面这个流程：

1. 先按 `domain + asset_type` 过滤
2. 再按 `tags / trigger_terms` 过滤
3. 再做向量召回
4. 最后按成功率和人工确认状态排序

#### 治理层

为每条资产记录：

- 使用次数
- 成功率
- 是否人工确认
- 版本号

这样已经足够工程化，而且不会一开始就过度设计。

### 21.9 面试时怎么说

推荐这样回答：

> 这类结构化资产不能简单堆成文档库，否则随着规模增长，检索效率和匹配准确率都会下降。我会把它设计成分层资产库，至少拆成指标定义、映射规则、查询模板和经验案例四类，并用关系型数据库加 JSON 字段存储，方便过滤、版本控制和质量追踪。检索上不会只靠向量召回，而是采用先分类、再标签过滤、再语义检索、最后重排序的混合检索流程。这样既能控制查询效率，也能提升语义匹配的稳定性。

### 21.10 一句话概括

> 这些资产不应该被当成普通知识文档，而应该被当成“结构化语义资产库 + 分阶段检索系统”来设计。

## 22. 简单 / 中等 / 复杂题目分别长什么样，怎么解，未解决题又长什么样

面试里如果只讲“这个项目有复杂题很难”，说服力不够。  
更好的方式是：

1. 说明不同难度题目在结构上有什么区别
2. 说明简单题 / 中等题通常怎么解
3. 说明复杂题为什么难、为什么容易失败
4. 用未解决样本举例说明问题到底卡在哪

### 22.1 简单题一般长什么样

简单题通常有这些特点：

- 单表或者两表
- 指标比较直接
- 时间条件清楚
- 没有复杂的留存 / 回流 / 滑窗逻辑
- 不涉及复杂的玩法分类映射

这类题更像：

> 已知表、已知字段、已知时间范围，做一次分组统计。

#### 简单题例子：付费区间统计

样本：`sql_44`

题目大意：

- 统计 2024 年付费用户
- 按付费区间划分
- 输出付费人数和付费金额

这类题的常规解法是：

1. 确定付费事实表
2. 在时间范围内按用户聚合付费金额
3. 用 `CASE WHEN` 做区间分桶
4. 按分桶做人数和金额聚合

这类题为什么相对容易：

- 用户主体清楚
- 时间窗口简单
- 业务规则就是区间映射
- 一般不需要多轮执行反馈

这类题的主要风险通常只有：

- 区间边界写错
- 金额单位换算错

### 22.2 中等题一般长什么样

中等题通常比简单题多一层复杂性，但还没到“业务规则缠绕”的程度。

常见特征：

- 两到四张表
- 有明确业务定义，但边界不算太深
- 需要做一点用户分层或状态判断
- 可能有简单的跨日 / 跨月对比

这类题更像：

> 先构造一个用户集合，再做统计。

#### 中等题例子：首日累充活动分 R

样本：`sql_61`

题目大意：

- 统计某天活动领奖玩家分 R
- 输出不同用户类型的活跃人数、首日付费人数、ARPU 等

这类题的常规解法通常是：

1. 先定义统计用户集合：哪些用户是“领奖玩家”
2. 再定义用户类型：
   - 历史非付费
   - 沉默付费
   - 小R / 中R / 大R / 超R
3. 再关联付费表和消耗表，做分层指标统计

为什么它比简单题难：

- 不是直接统计，需要先构造业务用户集合
- 用户类型本身就是一个语义层定义
- 需要跨表看“历史”和“当前”两个时间面

但为什么还不算最难：

- 口径虽然复杂，但总体还是一层用户分群 + 一层指标聚合
- 还没涉及多重时间窗口交织

### 22.3 复杂题一般长什么样

复杂题通常不是“SQL 更长”这么简单，而是业务问题本身有多个逻辑层。

常见特征：

- 多张表
- 多个时间窗口
- 需要先筛用户，再做行为分析，再做周期统计
- 玩法映射、位图、分层、留存、回流等规则混在一起
- 一旦某一层定义错，最终结果就全错

这类题更像：

> 先构造多个中间集合，再按业务口径逐层汇总。

#### 复杂题例子：网吧特权用户指标数据

样本：`sql_20`

题目大意：

- 统计网吧特权用户按月的人均对局时长
- 网吧时间占比
- 排位模式时长
- 组队率
- 付费率
- ARPA / ARPPA
- 高校特权激活率

这类题为什么复杂：

1. 涉及多个事实表
   - 登录
   - 对局
   - 付费
   - 特权相关表

2. 指标不止一个
   - 每个指标口径都不一样

3. 可能有 ID 体系不一致问题
   - `iuserid`
   - `vroleid`

4. 最终是一个月级汇总表
   - 但很多底层指标必须先按用户、按日算

这类题的正确解法通常是分层：

1. 先定义网吧特权用户集合
2. 再按月聚合登录与时长
3. 再按月聚合对局信息
4. 再按月聚合付费信息
5. 最后统一 join 成月级指标表

这类题最怕的是：

- join 主体错
- 先聚合后 join 的顺序错
- 某个分母口径和总体口径不一致

### 22.4 为什么复杂题容易“能跑但不对”

复杂题不一定会语法报错，反而最危险的是：

- SQL 能执行
- 结果有数据
- 但业务定义已经偏了

原因包括：

1. 用户集合定义错
2. 时间窗口定义错
3. 分类映射错
4. 聚合粒度错
5. 分母分子口径不一致

这也是为什么这个项目里真正困难的不是“生成 SQL”，而是“生成业务上正确的 SQL”。

### 22.5 已解决题一般是怎么被解决的

从项目经验看，已解决题通常满足下面一条或几条：

1. 业务定义相对直接
2. 数据表路径清楚
3. 过滤条件明确
4. 不需要太多中间集合
5. 人工能快速验证结果是否合理

一个很典型的成功模式是：

- 用 schema 限定可用表
- 用 few-shot 或相似样本给出结构参考
- 生成 SQL 后执行
- 如果报错或空结果，再做一次修正

对于简单和中等题，这套链路通常够用。

### 22.6 未解决题长什么样

未解决题最典型的样子不是“模型完全不会”，而是：

- 要么业务规则过深
- 要么需要多个中间层
- 要么某些定义隐含在题目里，没有显式结构化

下面给几个代表例子。

#### 未解决例子 1：空结果型

样本：`sql_54`

题目大意：

- `matchmode=1003`
- 统计每种玩法在周期内的日均参与人数和日均玩法次留

为什么没解决：

- 这题不是简单按日期和玩法上线时间匹配
- 真正关键是先构造“玩家实际参与某玩法”的用户集合
- 如果中间参与集合构错了，最后就容易空结果

这类题说明：

> 中间集合定义错误，比 SQL 语法错误更难发现。

#### 未解决例子 2：执行报错型

样本：`sql_88`

题目大意：

- 统计赛季末期垂类玩法轻中重度及独占用户
- 再看每周玩法分布和段位变化

为什么没解决：

- 玩法分类很复杂
- 同时涉及周期切分、活跃分层、段位信息
- 多层 CTE 叠加后直接执行失败

这类题说明：

> 一旦业务定义复杂到需要多层结构推导，生成阶段的稳定性就明显下降。

#### 未解决例子 3：能执行但语义覆盖不完整

样本：`sql_120`

题目大意：

- 统计 2025.7.24~7.25 持续活跃
- 以及 7.24 活跃、7.25 流失到盘外的 QQ 账号性别人数

为什么没解决：

- 题目本身包含两个状态群体
- 现有 SQL 只覆盖了其中一种固定标签
- 也就是 SQL 能跑，但语义没有把题目完整表达出来

这类题说明：

> 复杂题的失败不一定是逻辑错到底，而可能是只做对了一半。

#### 未解决例子 4：周期和排名逻辑塌缩

样本：`sql_80`

题目大意：

- 每 7 天为一个周期
- 统计上周没参与对决冠军、下周参与的用户
- 看上下周主玩玩法、参与玩法及对决冠军玩法时长

为什么没解决：

- 这题应该天然输出多个周期
- 但现有结果只出 1 行
- 说明周期展开和最终聚合粒度没有真正落地

这类题说明：

> 排名、周期、滑窗类问题非常容易“看起来 SQL 很完整，但最后粒度塌缩”。

### 22.7 一个更有说服力的面试表达方式

你可以这样概括不同难度题：

> 简单题一般是单表或双表的直接聚合，中等题通常要先构造一个业务用户集合再做统计，复杂题则往往涉及多个时间窗口、用户分层、玩法映射和多表 join。这个项目里真正难的不是复杂题 SQL 更长，而是复杂题需要先把业务问题拆成多个中间集合和语义规则，否则就容易出现 SQL 能执行但结果不对，或者只覆盖了一部分业务含义。 

### 22.8 一句话概括

> 简单题难在写对字段，中等题难在构造对用户集合，复杂题难在把业务口径拆成多层中间逻辑并保持整体一致。
